#include <stdio.h>
#include <stdlib.h>

void print(char *str) {
//    printf("a\n");
    exit(0);
}
//_Noreturn
void exit_(int status) {
    int a[10];
    for (int i = 0; i < 10; i++) {
        a[i] = i;
    }
//    __builtin_unreachable();
}
